FROM python:3.12-slim
WORKDIR /app
RUN pip install uv
COPY pyproject.toml uv.lock ./
COPY src ./src
RUN uv sync --frozen --group dev
EXPOSE 8000
CMD ["uv", "run", "uvicorn", "ego_dashboard.__main__:app", "--host", "0.0.0.0", "--port", "8000"]
